'use strict';
(() => {const R=window.AgentBridge.register;
R('html.title',a=>(String(a.text).match(/<title[^>]*>([\s\S]*?)<\/title>/i)||[])[1]?.trim()||null);R('html.links',a=>[...String(a.text).matchAll(/<a\b[^>]*href=["']([^"']+)["']/gi)].map(m=>m[1]));R('html.images',a=>[...String(a.text).matchAll(/<img\b[^>]*src=["']([^"']+)["']/gi)].map(m=>m[1]));R('html.ids',a=>[...new Set([...String(a.text).matchAll(/\bid=["']([^"']+)["']/gi)].map(m=>m[1]))]);R('html.classes',a=>[...new Set([...String(a.text).matchAll(/\bclass=["']([^"']+)["']/gi)].flatMap(m=>m[1].split(/\s+/)))]);R('html.strip_tags',a=>String(a.text).replace(/<script[\s\S]*?<\/script>/gi,'').replace(/<style[\s\S]*?<\/style>/gi,'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim());R('code.todo',a=>String(a.text).split(/\r?\n/).map((x,i)=>({line:i+1,text:x})).filter(x=>/\b(TODO|FIXME|HACK|XXX)\b/i.test(x.text)));R('code.functions',a=>{const r=/\b(?:function\s+([A-Za-z_$][\w$]*)|(?:const|let|var)\s+([A-Za-z_$][\w$]*)\s*=\s*(?:async\s*)?\(|def\s+([A-Za-z_]\w*)\s*\()/g,o=[];let m;while(m=r.exec(String(a.text)))o.push(m[1]||m[2]||m[3]);return[...new Set(o)]});R('code.imports',a=>String(a.text).split(/\r?\n/).filter(x=>/^\s*(?:import\b|from\s+\S+\s+import\b|const\s+\S+\s*=\s*require\()/i.test(x)));R('code.bracket_balance',a=>{const st=[],pairs={')':'(',']':'[','}':'{'},open='([{';for(const c of String(a.text)){if(open.includes(c))st.push(c);else if(c in pairs&&st.pop()!==pairs[c])return{balanced:false}}return{balanced:!st.length,remaining:st}});R('code.lines_nonempty',a=>String(a.text).split(/\r?\n/).filter(x=>x.trim()).length);R('code.find_duplicates',a=>{const m=new Map();String(a.text).split(/\r?\n/).forEach((x,i)=>{if(!x.trim())return;const v=m.get(x)||[];v.push(i+1);m.set(x,v)});return[...m].filter(([,v])=>v.length>1).map(([text,lines])=>({text,lines}))});
})();
